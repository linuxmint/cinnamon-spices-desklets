{
    "show-background": {
        "type": "checkbox",
        "default": true,
        "description": "Show background"
    },
    "bg-opacity": {
        "type": "scale",
        "default": 40,
        "min": 0,
        "max": 100,
        "step": 5,
        "description": "Background opacity (%)",
        "dependency": "show-background"
    }
}